# PARALLAX 지휘통제 콘솔 — 웹 판

macOS 앱(`~/Desktop/ParallaxConsole`, SwiftUI)을 **그대로 웹으로 옮긴 것**입니다.
창 배치·화면 구성·글자 크기·반응형 규칙까지 앱 쪽을 기준으로 맞췄습니다.

---

## 여는 법

`index.html` 을 더블클릭하면 됩니다. 크롬·사파리 모두 됩니다.

- 서버를 띄울 필요 없습니다 (`file://` 로 바로 열립니다).
- **인터넷이 있어야 지도가 나옵니다** — 지도 타일과 지도 라이브러리를 받아 옵니다.
- 공간 3D 는 `space/` 폴더를 읽습니다. **폴더째 같이 옮기세요.**

한 파일로 만들고 싶으면:

```
python3 한파일로_묶기.py
```

CSS·JS·CCTV 사진이 모두 들어간 HTML 한 개가 만들어집니다.
단 3D 스캔(94MB)은 넣지 않으므로, 그 파일도 `space/` 폴더와 같은 자리에 두어야 3D 가 뜹니다.

---

## 앱과 다른 점 (어쩔 수 없는 것)

| | 왜 |
|---|---|
| **지도가 애플 지도가 아닙니다** | 브라우저에서 애플 지도를 쓰려면 **유료** 개발자 키가 필요합니다. 대신 MapLibre + OpenFreeMap 벡터 지도를 쓰고, **주변 장소 표시를 끄고 길·행정구역 이름만 남기고**, 3D 는 카메라를 기울여 건물을 세워 앱과 같게 맞췄습니다. |
| **장소 검색** | 애플의 검색 대신 공개 지명 검색(Nominatim)을 씁니다. 결과 순서가 조금 다를 수 있습니다. |
| **아이콘** | SF Symbols 는 애플 것이라 웹에서 쓸 수 없습니다. 같은 모양을 벡터로 다시 그렸습니다. |
| **유리 재질** | `backdrop-filter` 로 흉내 냈습니다. 브라우저에 따라 흐림 정도가 조금 다릅니다. |
| **내 위치** | 브라우저가 권한을 묻습니다. `file://` 에서는 크롬이 막는 경우가 있어, 그럴 때는 서버로 띄우거나 사파리로 여세요. |

## 단축키 (앱과 같습니다)

| | |
|---|---|
| ⌘1 ~ ⌘5 | 앱 열기 (전체 상황 · 작전 지도 · 공간 3D · 현장 정보 전송 · 기록·회의록) |
| ⌘` / ⌘⇧` | 다음 창 · 이전 창 |
| ⌘W | 창 닫기 |
| ⌃⌘2 / ⌃⌘4 / ⌃⌘R | 2분할 · 4분할 · 위치 초기화 |
| ⌃⌥← / → / ↑ | 포커스된 창을 왼쪽 절반 · 오른쪽 절반 · 전체로 |

## 파일

```
index.html          껍데기와 아이콘 모음
theme.css           색·글자·간격 (Theme.swift)
app.css             상태바·런처·창·작업표시줄 (ContentView.swift)
core.js             공용 도구와 상태 (ConsoleStore, TileMetrics)
tiling.js           창 관리자 (WindowSystem.swift)
shell.js            껍데기 그리기와 단축키
data.js             사건·인력·차량·CCTV·경로 자료 (SampleData.swift)
screen-*.js/css     화면 다섯 개
space/              공간 3D 뷰어와 스캔 (원본 묶음에서 가져온 것)
cctv_street.jpg     CCTV 화면 사진
```

## 주의

- 사건·인력·좌표는 모두 **가상**입니다. 지도만 실제입니다.
- 스캔 한 개(`20260914-135539`)는 실제 집 내부입니다. 팀 밖으로 공유하지 마세요.
